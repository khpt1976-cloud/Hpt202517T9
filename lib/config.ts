// Configuration for external services
export const config = {
  // Other service configurations can be added here
  services: {
    // Example: API endpoints, external integrations, etc.
  }
}

export default config